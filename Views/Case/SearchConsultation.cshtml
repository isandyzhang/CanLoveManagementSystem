@{
    ViewData["Title"] = "查詢個案 - 會談服務";
    ViewData["Breadcrumbs"] = new List<(string Text, string Url)>
    {
        ("個案管理", Url.Action("Index", "Case") ?? string.Empty),
        ("查詢個案", Url.Action("SearchForOpenCase", "Case") ?? string.Empty),
        ("會談服務", string.Empty)
    };
}

<div class="w-full px-4 sm:px-6 lg:px-8">
    <div class="max-w-[1400px] mx-auto">
        @{ ViewBag.CurrentPage = "Search"; ViewBag.CurrentTab = "Consultation"; }
        <partial name="~/Views/Case/Partials/_CaseTabs.cshtml" />
        <div class="card bg-white dark:bg-white shadow-card-lg border rounded-xl border-[#93C5FD]">
            <div class="px-6 py-6">
                <div class="text-slate-600">此分頁尚未實作，未來將提供「會談服務紀錄」的查詢功能。</div>
            </div>
        </div>
    </div>
</div>


