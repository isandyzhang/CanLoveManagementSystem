@{
    ViewData["Title"] = "新增關懷訪視記錄表";
    // 自訂麵包屑：個案管理 > 新增個案 > 關懷訪視記錄表
    ViewData["Breadcrumbs"] = new List<(string Text, string Url)>
    {
        ("個案管理", Url.Action("Index", "Case") ?? string.Empty),
        ("新增個案", Url.Action("Create", "Case") ?? string.Empty),
        ("關懷訪視記錄表", string.Empty)
    };
}

<div class="w-full px-4 sm:px-6 lg:px-8">
    <div class="max-w-[1400px] mx-auto">
        @{ ViewBag.CurrentPage = "Create"; ViewBag.CurrentTab = "CareVisitRecord"; }
        <partial name="~/Views/Case/Partials/_CaseTabs.cshtml" />
        <div class="card bg-white dark:bg-white rounded-xl shadow-card-lg border border-[#F9A8D4]">
            <div class="p-6">
                <div class="text-center py-10 text-slate-500">
                    <i class="bi bi-inbox text-slate-400" style="font-size: 3rem;"></i>
                    <h4 class="mt-3">功能開發中</h4>
                    <p>關懷訪視記錄表功能正在開發中，敬請期待</p>
                </div>
            </div>
        </div>
    </div>
</div>