@model CanLove_Backend.Models.Mvc.ViewModels.CaseFormViewModel

<div class="mt-6 flex justify-end gap-3">
    <a asp-controller="Case" asp-action="Index" class="btn btn--secondary">
        <i class="bi bi-arrow-left me-1"></i>返回
    </a>

    @if (Model.Mode == CanLove_Backend.Models.Mvc.ViewModels.CaseFormMode.Create)
    {
        <input type="hidden" name="SubmitAction" value="Submit" />
        <button type="submit" class="btn">
            <i class="bi bi-check-circle me-1"></i>提交審核
        </button>
    }
    else if (Model.Mode == CanLove_Backend.Models.Mvc.ViewModels.CaseFormMode.ReviewEdit)
    {
        <button type="submit" name="SubmitAction" value="Approve" class="btn">
            <i class="bi bi-check2-circle me-1"></i>通過
        </button>
        <button type="submit" name="SubmitAction" value="Reject" class="btn btn--secondary">
            <i class="bi bi-x-circle me-1"></i>不通過
        </button>
    }
    else if (Model.Mode == CanLove_Backend.Models.Mvc.ViewModels.CaseFormMode.ReadOnly)
    {
        <input type="hidden" name="SubmitAction" value="None" />
    }
</div>


