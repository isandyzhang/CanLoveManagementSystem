@{
    ViewData["Title"] = "新增會談服務紀錄表";
    // 自訂麵包屑：個案管理 > 新增個案 > 會談服務記錄表
    ViewData["Breadcrumbs"] = new List<(string Text, string Url)>
    {
        ("個案管理", Url.Action("Index", "Case") ?? string.Empty),
        ("新增個案", Url.Action("Create", "Case") ?? string.Empty),
        ("會談服務記錄表", string.Empty)
    };
}

<div class="flex justify-between items-center mb-4">
    <h2 class="text-2xl font-semibold text-slate-900 flex items-center gap-2">
        <i class="bi bi-file-earmark-text"></i>新增會談服務紀錄表
    </h2>
</div>

<div class="card bg-white dark:bg-white rounded-xl shadow-card-lg border-0">
    <div class="p-6">
        <div class="text-center py-10 text-slate-500">
            <i class="bi bi-inbox text-slate-400" style="font-size: 3rem;"></i>
            <h4 class="mt-3">功能開發中</h4>
            <p>會談服務記錄表功能正在開發中，敬請期待</p>
        </div>
    </div>
</div>